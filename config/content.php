<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Editable Content Locales
    |--------------------------------------------------------------------------
    |
    | These locales drive multilingual content editing in admin editor
    | responses. They are intentionally separate from interface locale
    | resolution so product content can support more languages than the
    | admin UI message catalog.
    |
    */
    'editable_locales' => ['ar', 'en'],

    'default_locale' => 'en',
];
